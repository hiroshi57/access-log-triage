# /etc/fail2ban/filter.d/wordpress-auth.conf
#
# wp-login.php / xmlrpc.php への POST を「アクセスログ」から拾うフィルタ。
# 同梱の nginx 系 jail はエラーログを見るため、アクセスログにしか出ない
# ログイン総当たりはこの自作フィルタで拾う。
[Definition]
failregex = ^<HOST> .* "POST [^"]*/(wp-login\.php|xmlrpc\.php)
ignoreregex =
