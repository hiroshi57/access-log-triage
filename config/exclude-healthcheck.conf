# exclude-healthcheck.conf
#
# 死活監視のヘルスチェックで 1 日数万行が埋まると集計が読みにくくなる。
# 監視パスだけログに書かない設定例。

# --- nginx ---
# server {} 内に置く。
#   location = /healthz {
#       access_log off;
#       return 200;
#   }

# --- Apache ---
# 条件付きロギング(env= で除外)。
#   SetEnvIf Request_URI "^/healthz$" dontlog
#   CustomLog ${APACHE_LOG_DIR}/access.log combined_realip env=!dontlog
